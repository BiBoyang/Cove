# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode
build/
DerivedData/
*.xcodeproj
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.moved-aside
*.hmap
*.ipa
*.xcarchive

# Swift Package Manager
.build/
.swiftpm/xcuserdata/

# CocoaPods / Carthage (not used, ignored defensively)
Pods/
Carthage/Build/

# Vendored libmpv dylib forests (licensing-sensitive, never committed):
#   Vendor/libmpv/             active forest — self-built LGPL-clean chain
#                              (scripts/build-libmpv.sh; license texts and
#                              upstream source mapping live in LICENSES/)
#   Vendor/libmpv-self/        build-libmpv.sh's own output/staging dir
#   Vendor/libmpv-iina-backup/ rollback copy of the old IINA.app forest,
#                              kept during acceptance (assemble-libmpv.sh,
#                              deprecated)
Vendor/

# Local-only collaboration logs (AI-assisted dev process). Design knowledge
# lives in plans/, which stays tracked.
sessions/
