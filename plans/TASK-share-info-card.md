# TASK: share 卡片升级信息卡片

日期：2026-09-08 ｜ 协作模式（用户实现 + 助手规划/Review，协议 WORKFLOW.md）
上游：design/DESIGN-TOKENS.md §6.7（share 卡片配方，含"可承载元信息"
半成品表述）+:185 待定拍板项（本卡即立项拍板）

## 目标复述

share 网格卡片从"图标 + 名称"入口卡升级为信息卡片：加类型标签与相对
时间等元信息行。视觉配方沿用既有令牌（surface/hover-fill/描边三族已
拍板），不新设计视觉语言；参照 SenPlayer 服务器卡片（02 图）的信息
层级。

## 决策记录（已拍板 2026-09-08）

- 升级方向：**A 信息卡片**（否决 B 维持极简）。

## Step 列表与 DoD

### Step 0（spike，先行）：元数据来源确认
- 核查 SMB share 枚举接口返回的字段（SRVSC 枚举通常只有名称/类型/
  注释——相对时间大概率无源）。若时间无源：拍板降级（仅类型标签 +
  注释/拼接入口状态）或另寻数据源（如最近打开记录），回来更新本卡再实施。
- DoD：来源结论一行话 + 证据（接口返回结构）。

### Step 1 数据供给 + 卡片布局
- 按 Step 0 结论供给元信息；卡片加元信息行（类型标签 caption 档 +
  相对时间 caption 档，令牌字号）；网格行高/卡片尺寸随内容调整。
- DoD：build 零警告、test 全绿；前后截图对照（前图
  plans/UI-AUDIT-2026-09-05/25-sharegrid.png）；真机验收：信息可读、
  不拥挤、hover/选中观感不回归。

## 风险与回滚点

- 元数据无源是最大不确定性——Step 0 就是为此设的，先探后建。
- 卡片变高影响网格节奏（sectionInset/行距令牌已就位，直接用）。
- 每 Step 单 commit 可 revert。
