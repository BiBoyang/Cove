# Session 2026-08-22

## 今日完成

- 完成 Cove Reader 路线 A1：新增 `PagedReaderWindowController`，单页黑色背景、居中等比图片、上一张/下一张按钮、左右键/PageUp/PageDown、页码和 Esc 退出。
- 将 Reader 图片加载管线提取为共享 `ReaderImageLoader`；保留 display/original 双池缓存、取消和 generation guard，避免陈旧任务覆盖当前页。
- `MainWindowController` 切换到 A1 单页 Reader；目录图片和 CBZ 均支持手动翻页，双击图片从当前图片索引打开。
- 删除目录浏览和 Reader 入口的自动图片预热调用；A1 不触发当前目录或相邻页面预取，仅保留用户在设置中主动配置的 `preheatFolders` 预热。
- 修复 `NSImage(cgImage:size: .zero)` intrinsic size 可能压过窗口上限约束导致的横屏裁切；降低 `NSImageView` compression resistance 后，横屏/竖屏均人工确认等比居中无裁切。
- CBZ 单页阅读、边界按钮、快速翻页和基础关闭流程已人工验证通过。
- 基础校验通过：`make build`（零 Swift warning）、`make test`（Frameworks 与 smb-spike 全绿）。
- 完成架构重构第二阶段：Browser/Servers/Preferences 迁入 Features，MainWindowController 降为窗口壳，新增 LibraryCoordinator；新增 Browser/Server/Share/Preferences ViewModel。
- 新增 `CoveTests` App test target，使用 Swift Testing 覆盖浏览器过滤排序、Server/Share 状态、Preferences 输入校验、Reader 边界和陈旧任务丢弃；`make test` 全绿。
- 完成架构审计文档：`plans/ARCHITECTURE-AUDIT-2026-08-22.md`，记录当前依赖边界和未来 target/module 候选。
- 修复 Preferences 缓存统计/清理的陈旧任务回写竞态，补齐 generation guard、清理失败提示和确定性并发测试。
- 发布版本确定为 `0.2.0`（build `2`），准备 annotated tag `v0.2.0`。

## 关键决策

- A1 明确采用单页手动翻页，不继续修复连续纵向条带，不引入 slot、retention、anchor 或相邻页预加载。
- 严格关闭目录级和 Reader 级自动预热；相邻页预加载与目录预热恢复推迟到 A2 重新设计。
- 保留用户主动配置的文件夹预热能力，与 Reader 分页策略分离。

## 遗留问题

- A2：在 A1 稳定基线之上重新讨论相邻页预加载。
- 更后续再讨论连续纵向条带 Reader。
- `v0.2.0` 发布链路尚未执行 commit/tag/push，等待草案确认。

## 备注

- `v0.2.0` 作为当前稳定小版本，包含 A1 Reader、CBZ、缓存/缩略图/预热设置、MVVM/Coordinator 分层、ReaderKit 与 App 测试。
