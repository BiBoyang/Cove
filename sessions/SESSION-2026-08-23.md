# Session 2026-08-23

## 今日完成

- 全项目架构评审（重点架构）：确认分层/依赖规矩真实落地；修复四轮问题并全部推送。
- 缓存键与解码预算收编：`CacheKey.sourceFile` 工厂（CacheKit）收编 epoch-0 与 variant 约定；`maxPixelSize`/`maxDecodedPixelSize` 移入 ImagePipeline（`ee1d6af`）。
- 删除零调用公开 API（`CacheService.usage/clearAll`、`PreheatService.submit*`、`ImagePipeline.dimensions`）（`c9f3ea1`）。
- 噪音过滤收敛进 SourceKit 并推广为「所有 `.` 开头条目」：`isMacOSNoise` → `isNoise`（`b7f26be`、`08e3dab`）；修复 `/`-rooted SMB 路径下 `__MACOSX` 漏过滤（`f74c3dc`）。
- 排序统一到 SourceKit `NaturalSort`（`.numeric + .caseInsensitive`，locale 无关），浏览器与 CBZ 页序同一语义（`dffbbca`）。
- Coordinator 测试补齐：`LibraryNavigationPath` 纯值类型抽取 + Reader open 竞态注入缝，CoveTests 10→17（`e48dd8f`）。
- 服务层显式注入：三个单例全删，NotificationCenter 换 `addChangeObserver`，AppDelegate 成为唯一 composition root（`79eacf3`）；`AddServerViewModel` 抽取（`57514a9`）。
- A2 文件夹点击预热：能力层（`cancel(priority:)` 定向取消、单层枚举）+ 工具栏按钮三态进度（`7eb9d09`、`4011087`），真机验收通过。
- 递归预热（协作模式：本会话规划/Review，另一会话实现）：两阶段方案（枚举完一次性入队），撞顶 `truncatedAtCap` 透传至完成文案；修复 `collectImages` 吞取消的 bug；删除无消费方的 `listImages`（`ee8b675`、`65e675f`、`92224aa`）。
- 推送 `v0.2.0` tag（指向 A1 基线 `3345cb8`）。

## 关键决策

- 递归预热选两阶段（枚举完再入队）而非流式：枚举是元数据 list（秒级），下载才是分钟级，流式要动四层接口不值。
- 预热上限沿用 5000/1000 不放宽；撞顶必须在完成文案可见（`truncatedAtCap` 随状态透传，View 不直连 PreheatKit）。
- 隐藏文件策略：不做名单制，按 Unix 惯例隐藏所有 `.` 开头条目。

## 遗留问题

- 递归预热真机目视验收（三态、切目录自动取消、撞顶文案）待用户确认。
- 相邻页低配预取（`.immediate` 队列）候选下一步。
- Preheat 解码串行（actor 上跑 CPU 解码）仅注释记录，待实测慢再挪。
- 代理对 git POST 间歇断连；绕过代理直连可推，未做永久配置。

## 备注

- 发布 v0.3.0：递归预热 + 架构清理共 16 个 commit 自 v0.2.0。
